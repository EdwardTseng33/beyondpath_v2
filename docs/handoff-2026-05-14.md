# Session Handoff · 2026-05-14

**From**: 🌸 蘇菲（5/14 一整天 session）
**To**: 下個 session 的蘇菲 + 霍爾（Codex 端）
**Trigger**: Edward 命「先交接 session」

---

## 0 · 一句話狀態

**`beyondpath.tw` 上線完、Supabase + Google OAuth 串通、worker apply / client intake 雙邊 e2e 寫入 DB · 6 council audit 全跑完 + 10 條城堡側 P0 已 ship · 剩 4 件 Edward 自己動的（共 70 分鐘）即可進 Soft Launch GO**

Production URL：[`https://beyondpath.tw`](https://beyondpath.tw)、commit `ad64923` 上 main。

---

## 1 · 本 session（5/14 一整天）按時序完成的事

### 早段 · 早期上線決策 + Supabase + OAuth 接通

1. Edward 問「POC 整個跑通了嗎、能上 BD 嗎、能 Google 登入嗎、撇開金流先上線」→ 我盤點 prototype 純前端 demo、沒後端、決定走 Supabase 一條龍最快路徑
2. Edward 給 Supabase **publishable key + secret key**（`iacwmkcloxjffghrweie.supabase.co`）
3. 我用 Chrome MCP 代 Edward 操作：
   - GCP project `moving-castle-488905` 建 OAuth client `BeyondPath Web` + redirect URI `https://iacwmkcloxjffghrweie.supabase.co/auth/v1/callback`
   - Supabase Auth → Providers → Google enable + 貼 client ID/secret + save
   - 停用遺失的舊 secret（GCP 新版 secret 只在建立當下顯示一次）
4. 寫 `supabase/migrations/001_initial_schema.sql` 建 3 表：`profiles` / `worker_applications` / `client_intakes` + RLS（但 anon insert 撞 RLS、prototype 階段先 disable）
5. 接 `components/supabase.js`：`window.bpAuth` / `bpWorkerApply` / `bpClientIntake`
6. `sign-in.html` 砍 fake auth、改真 `bpAuth.signInWithGoogle`
7. Worker apply Step 3 Submit → 真實寫 `worker_applications`（不再 mailto）
8. Client intake Step 04 → `IntakeSubmitModal` 收 email → 寫 `client_intakes`
9. E2E 驗收：test rows insert 成功 → 清掉

### 中段 · beyondpath.tw 主域上線

10. Edward 戳「不要寄 vercel.app 子域 + 不要 spec、要 path」→ 解釋 GitHub Pages 整域接管限制、推 buy `beyondpath.tw` 主域
11. Edward 5 分鐘買好 `beyondpath.tw`
12. 我代操作 GoDaddy DNS：
    - A `@` → `76.76.21.21`（Vercel apex IP）
    - CNAME `www` → `beyondpath.tw.`（chain 到 apex · `cname.vercel-dns.com` 被 Chrome MCP JWT 過濾擋了、但 chain 也通）
    - 刪 WebsiteBuilder placeholder A record
13. Vercel CLI 加 `beyondpath.tw` + `www.beyondpath.tw` 為 custom domain
14. 驗證：DNS propagate 完 + SSL auto + `/landing.html` / `/app.html` / `/sign-in.html` 全 200 OK

### 後段 · 6 council 全 audit + P0 fixes ship

15. Edward 命「接續上線、城堡規劃完善、不出紕漏」→ 派 6 council 並行 deep-dive：
    - 🔥 卡西法（tech audit）→ `01-tech-audit-calcifer.md` + `02-rls-hardening.sql`
    - 🧙‍♀️ 沙利曼（trust + 合規）→ `02-trust-audit-suliman.md`
    - 🌿 馬魯克（QA + entry inventory）→ `03-qa-launch-checklist-markl.md`
    - 🧙 霍爾（產品 roadmap + risk register）→ `04-launch-roadmap-howl.md`
    - 🔮 女巫（設計 + a11y）→ `05-design-audit-witch.md`
    - 🥕 蕪菁頭（UX + drop-off）→ `06-ux-audit-turnip.md`
16. 整合 → 寫 `00-launch-plan-sophie.md`（17 條 must-fix · P0/P1/P2 分級）
17. Ship 城堡側 10 條 P0：
    - `vercel.json` root redirect
    - app/index/landing React 18.3.1 → production.min.js + 新 SRI
    - `app2.jsx` Topbar labels 順序修（Pre-intake / Confirm / AI Parse / Match）
    - `landing.html` Prototype banner 改 Early Beta 文案
    - `worker.jsx` submitted state 文字修（Supabase 已收 + 24h 回信）
    - `styles.css` `--muted` `#7a786f` → `#9a9aa3`（WCAG AA pass）
    - `styles.css` 全域 `*:focus-visible` rule
    - `shell.standalone.jsx` logout 加 `await bpAuth.signOut()` 清 session
    - cache-bust 全 entry bump `v0.8.0-launch`
18. Commit `ad64923` push + Vercel CLI 手動 deploy → prod live

---

## 2 · 當前 Gate 狀態（v0.8.0-launch）

| Gate | 內容 | 狀態 |
|---|---|---|
| **Gate 1 · 流程測試**（卡西法）| Chrome MCP 4 entry smoke + 0 console error | ⏳ 部分（landing OK · 4 entry 完整 smoke 未跑）|
| **Gate 2 · 視覺檢修**（女巫）| Token + 一致性 + a11y | 🟡 7 C 中 3 修完（C1 muted、C5 focus-visible、C7 partial）· 還剩 4（C2 mobile bp-jtrack / C3 mobile.html banner / C4 IntakeSubmitModal token / C6 aria-label）|
| **Gate 3 · 遠景校準**（霍爾）| Edward 拍板 7 項 + ADR 落檔 | ⏳ 等 Edward「都採用 ⭐」 |
| **Gate 4 · 版控確認**（馬魯克）| 版號 + diff + rollback | ✅ v0.8.0-launch tag 未打、但 commit `ad64923` 可作 baseline |
| **Gate 5 · 信任關卡**（沙利曼）| RLS + Site URL + 2FA + §27 | 🔴 全等 Edward（70 分鐘可動完）|

**Soft Launch GO 條件**：Edward 動完 4 件 + Gate 2 剩餘 4 C 修完 + Chrome MCP 4 entry smoke 完整跑 → 即可進 soft launch（W4 內、最快 2026-06-09）。

---

## 3 · Edward 仍待動的（4 件 critical + 7 拍板 + 公司 / 律師）

### 🔴 Critical · 今天可動完（70 分鐘）

1. **跑 `docs/launch/02-rls-hardening.sql`** · Supabase SQL Editor 貼 → Run（30 秒）
   - 不修 = DB anonymously 可讀可改、所有 lead email 公開
2. **Supabase Auth Site URL + Redirect URLs 加 beyondpath.tw** · [URL Configuration](https://supabase.com/dashboard/project/iacwmkcloxjffghrweie/auth/url-configuration)（30 秒）
   - 不修 = Google 登入完跳不回 beyondpath.tw
3. **Gmail 2FA 開** · edwardt0303@gmail.com 是 RLS admin email、被釣等同 DB 公開（10 分鐘）
4. **回霍爾 7 拍板項**（`04-launch-roadmap-howl.md §5`）：公司結構 / 律師 / Resend / Worker 名單 / Client BD / DPA / 揭露程度——每件都有 ⭐ 建議、Edward 可一鍵「都採用 ⭐」（5 分鐘）

### 🟠 短中期（W1-W7 分散）

- 公司開設（找代辦、5-10 工作天）
- §27 個資內部計畫文件填空（沙利曼 `02-trust-audit-suliman.md §4` 已給 template、Edward 1 小時自填）
- 律師合約 review（NT$ 30-50K、W3-W4 內 ship）
- Resend 帳號開 → API key 給蘇菲接 email 通知

---

## 4 · 下個 session 進來該做的（priority order）

### 動作 A · 接 Edward 回覆

- 若 Edward 回「都採用 ⭐」 → 寫 `project_beyondpath.md` 內 ADR-001 ~ ADR-007、然後動 W1 任務
- 若 Edward 改某幾條 → 整合改動再寫 ADR
- 若 Edward 給 Resend API key → 接 Edge Function 寄 email 通知（每次 submit 寄到 edwardt0303@gmail.com）

### 動作 B · Gate 2 剩 4 C（女巫 backlog）

- C2 `app.html:128` `.bp-jtrack-inner` mobile 12-col 碎裂 → 加 `@media (max-width: 768px)` 6-col fallback
- C3 `mobile.html:17` disclaimer banner 蓋畫面 → body padding 改 `60px 16px 32px`
- C4 IntakeSubmitModal（`app2.jsx:1021-1069`）token 化 → 抽 `.bp-modal` / `.bp-modal-card` 共用 class
- C6 sign-in role select + worker.jsx ApplyProgress + IntakeSubmitModal 加 `aria-label` + `role="dialog"`

### 動作 C · Gate 1 完整 smoke

派卡西法 Chrome MCP 跑 4 entry 完整 user journey：
- `beyondpath.tw/landing.html` · scroll 全 + 5 CTA + footer link
- `beyondpath.tw/app.html?role=client&step=0` · 4 step 走完 → modal submit（mock email）
- `beyondpath.tw/app.html?role=worker&onboarding=1` · 3 step 走完 → preview 出能力卡 → submit
- `beyondpath.tw/sign-in.html` · Google OAuth 真實跳 + 回 dashboard

每 entry 0 console error = Gate 1 PASS。

### 動作 D · UX P1（蕪菁頭 backlog）

- F-04 預算滑桿預設 NT$240K → NT$100K（`app2.jsx:1092`）
- F-05 Client intake `sessionStorage` 持久化（refresh/back 不歸零）
- F-03 Worker generate `sessionStorage` + 加「中斷可回來繼續」說明

---

## 5 · 重要 reference docs（完整 inventory）

### 本 session 新增（in `prototype-v0.2/docs/launch/`）

| 檔案 | 用途 |
|---|---|
| `00-launch-plan-sophie.md` | **5 分鐘版整合 plan · 17 must-fix · Edward 拍板項** ⭐ 進來先讀 |
| `01-tech-audit-calcifer.md` | 技術架構 audit · 含 production hardening |
| `02-rls-hardening.sql` | **Edward 要跑的 SQL** · RLS production policy |
| `02-trust-audit-suliman.md` | 信任 / 合規 / §27 template / DPA / TOS draft |
| `03-qa-launch-checklist-markl.md` | 12 entry inventory + 80 條 verification + rollback |
| `04-launch-roadmap-howl.md` | Launch readiness scorecard + W0-W7 timeline + 7 拍板項 |
| `05-design-audit-witch.md` | 設計 audit + 10 維 rubric + 7 C1-C7 |
| `06-ux-audit-turnip.md` | 雙 persona walkthrough + drop-off 預測 |

### 既有（仍 valid · 跨 session reference）

| 檔案 | 用途 |
|---|---|
| `docs/project-status.md` | Codex 端累積狀態紀錄（每次 ship 加一行） |
| `docs/collaboration-protocol.md` | Sophie ↔ Holl 分工 + verification checklist |
| `docs/handoff-2026-05-13-sophie-realign.md` | 5/13 reposition 對焦 |
| `docs/product-flow-competitor-review-2026-05-11.md` | 競品 + 11 P0-P2 feature |
| `docs/landing-market-review-2026-05-11.md` | landing 市場 review 72/100 |
| `docs/ai-workflow-proof-profile-spec.md` | Worker apply spec |
| `CLAUDE.md` / `AGENTS.md` / `README.md` | Codex 端 SSOT 入口 |
| `supabase/migrations/001_initial_schema.sql` | 3 張表 schema |

### 根目錄 `BeyondPath2.0/`（仍 valid · 跨 sprint reference）

- `BeyondPath_Product_Framework.md` v0.4 compact（產品框架 SSOT）
- `PRD_Product_Flow_v0.2.md` · `Trust_Compliance_Brief_Product_Flow.md` · `visual_dna_spec_v0.2.md` 等
- `archive/pre-realign-2026-05-11/` · 5/8-5/10 舊 spec 已歸檔、**不要當依據**

---

## 6 · 危險區 · 下個 session 不要做的事

- ❌ 不要重啟 GoDaddy DNS、不要改 A record 或 CNAME（已 work、改容易斷）
- ❌ 不要修 GCP OAuth client redirect URI 從 `iacwmkcloxjffghrweie.supabase.co/auth/v1/callback` 變成別的（OAuth callback 永遠在 supabase 域、不在 beyondpath.tw）
- ❌ 不要直接動 `archive/pre-realign-2026-05-11/` 內舊 spec 當依據（Tier A < 10% / escrow 30/30/40 / 試做案 都是舊定位、5/11 已 reposition）
- ❌ 不要在 BP2.0 根目錄寫新 spec / handoff（一律到 `prototype-v0.2/docs/`）
- ❌ 不要重 enable RLS without 跑 `02-rls-hardening.sql`（會把 anon insert 擋掉、worker / client submit 全壞）
- ❌ 不要在 prototype-v0.2 加 build system / Next.js / Vite（collaboration-protocol 鎖、React UMD + Babel CDN 維持）

---

## 7 · 城堡 P1 + P2 backlog（不擋 soft launch、下個 sprint 排）

### P1（影響首批轉換率）
- C2 mobile bp-jtrack（女巫）
- C3 mobile.html banner（女巫）
- F-04 預算錨定 240k → 100k（蕪菁頭）
- F-05 Client intake sessionStorage（蕪菁頭）
- F-03 Worker generate sessionStorage + 中斷說明（蕪菁頭）

### P2（上線後 sprint 1）
- C4 IntakeSubmitModal token 化（女巫）
- C7 Logo mark 統一三角 SSOT（女巫）
- C6 aria-label + role=dialog（女巫）
- F-09 Mobile Tier tooltip（蕪菁頭）
- F-08 免費 AI fallback 說明（蕪菁頭）
- F-07 「不需帳號」說明（蕪菁頭）
- Resend email 通知接（卡西法 + 霍爾、等 Edward API key）

### P3（soft launch 後 4 週內）
- 公司開設完成
- 律師合約 review ship
- §27 文件落地
- Supabase 自動備份 + Sentry / monitoring（沙利曼 + 卡西法）

---

## 8 · Token & 雙軌工時記錄

### 本 session 預估
- 主對話蘇菲：~700-900k token（agent dispatch 不算）
- 6 council 共：~600k token（並行）
- 整 session 合計：~1.4M token（接近 1M context 上限）

### 5/14 一整天等同工時
- Real time（一個資深 PM + 工程師團隊）：3-5 天工作
- 移動城堡實跑：8-10 小時 wall clock
- 倍率：~7-8×（高 audit 並行 + Edward 實際時間佔比低）

---

## 9 · 給 Edward 的一句話（下個 session 開頭可貼）

> 「Edward、你 5/14 動完 4 件 critical（70 分鐘）+ 回我 7 拍板項即可進 Soft Launch GO（最快 6/9）。城堡 P1 + P2 我下個 sprint 排、不擋你。」

---

*🌸 蘇菲 · session handoff · 2026-05-14 · 寫給下個 session 不要踩坑*
